# Backend: NumPy

The NumPy-based backend implementation. Uses native NumPy arrays and operations under the hood. Useful for debugging (supports float64) and as a reference implementation.

Activate by setting the environment variable before importing:

```bash
export TINY_PYTORCH_BACKEND=np
```

::: tiny_pytorch.backend_numpy
